# Memory Index

<!-- One line per file: a link to the file, then a one-line outcome/summary -->
<!-- Keep this list short. feedback/user files are durable behavioral rules — they stay.
     project files are ephemeral status — promote anything worth keeping to your durable
     knowledge store (docs, wiki, README), then trim the entry. This index should shrink
     after a cleanup pass, not grow. -->
